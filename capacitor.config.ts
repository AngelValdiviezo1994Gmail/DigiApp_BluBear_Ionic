import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'digi_app_ionic_blu_bear',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
